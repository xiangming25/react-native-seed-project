/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';
import Root from './app/Root';

AppRegistry.registerComponent('seedApp', () => Root);
